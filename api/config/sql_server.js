
const config = {
    server: "localhost",
    user: "sa",
    password:"p@ss2023",
    database: "Alumno",
    options: {
        trustServerCertificate: true
    }
}

module.exports.config = config;